[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# "app" is needed because the app modules import each other as `utils.<module>`.
pythonpath = [".", "app"]
